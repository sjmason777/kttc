# Краткий отчет: Улучшение NLP helpers для английского и китайского языков

**Дата:** 13 ноября 2025
**Цель:** Исследование современных NLP инструментов для улучшения EnglishLanguageHelper и ChineseLanguageHelper

---

## Основные вопросы и ответы

### 1. Что делает RussianFluencyAgent? Он всегда активен?

**Что делает:**
- Специализированная проверка беглости русского языка
- **Гибридный подход:** NLP (mawo-pymorphy3 + mawo-razdel) + LLM параллельно
- **Детерминированные проверки:**
  - Падежное согласование (6 падежей)
  - Вид глаголов (совершенный/несовершенный)
  - Использование частиц (же, ли, бы)
  - Регистр обращения (ты/вы)
  - Порядок слов
- **Anti-hallucination:** NLP верифицирует результаты LLM
- **Enrichment данные:** Морфологический контекст для LLM промптов

**Всегда ли активен?**
❌ **НЕТ!** Активируется только когда `target_lang == "ru"`

**Код активации** (`src/kttc/agents/dynamic_selector.py:311-324`):
```python
if task.target_lang == "ru":
    # Replace generic fluency with Russian-specific
    if "fluency" in agent_set and "fluency_russian" not in agent_set:
        agent_set.remove("fluency")
        agent_set.append("fluency_russian")
```

✅ **Вывод:** Система уже работает правильно!

---

### 2. Нужно ли создавать агенты для английского и китайского?

❌ **ПОКА НЕТ**

**Причины:**
1. Сначала нужно усилить helpers (добавить детерминированные проверки)
2. Текущие helpers слабые: `check_grammar()` возвращает пустой список `[]`
3. После улучшения helpers нужно протестировать на реальных данных
4. Только потом принимать решение о создании специализированных агентов

**Сравнение текущего состояния:**

| Язык | Детерминированные проверки | Библиотека | Статус |
|------|---------------------------|-----------|--------|
| **Русский** | ✅ Падежи, вид, частицы, род | mawo-pymorphy3 | ✅ Готово |
| **Английский** | ❌ Нет (пустой список) | spaCy | ❌ Слабый |
| **Китайский** | ❌ Нет (пустой список) | jieba + spaCy | ❌ Слабый |

---

### 3. Что нужно для улучшения helpers?

## Английский язык - ГОТОВЫЕ РЕШЕНИЯ ⭐⭐⭐⭐⭐

### LanguageTool (лучший вариант)

**Библиотека:** `language-tool-python` v2.9.5 (ноябрь 2025)

**Возможности:**
- ✅ 5,000+ грамматических правил для английского
- ✅ Subject-verb agreement (согласование подлежащего-сказуемого)
- ✅ Проверка артиклей (a/an/the)
- ✅ Согласование времён глаголов
- ✅ Проверка предлогов
- ✅ Определение пассивного залога
- ✅ ~200 MB, работает offline

**Установка:**
```bash
pip install language-tool-python
```

**Требования:**
- Python 3.9+
- Java 17.0+
- ~200 MB дискового пространства

**Пример использования:**
```python
import language_tool_python

tool = language_tool_python.LanguageTool('en-US')

text = "He go to school every day"
matches = tool.check(text)
# Найдёт: subject-verb agreement error

corrected = tool.correct(text)
# "He goes to school every day"
```

**Интеграция в EnglishLanguageHelper:**
```python
def check_grammar(self, text: str) -> list[ErrorAnnotation]:
    """Check English grammar using LanguageTool."""
    if not self._language_tool:
        return []

    matches = self._language_tool.check(text)
    errors = []

    for match in matches:
        errors.append(ErrorAnnotation(
            category="fluency",
            subcategory=f"english_{match.ruleId}",
            severity=self._map_severity(match),
            location=(match.offset, match.offset + match.errorLength),
            description=match.message,
            suggestion=match.replacements[0] if match.replacements else None
        ))

    return errors
```

**Оценка:** ⭐⭐⭐⭐⭐ Готово к продакшену, высокая отдача

---

## Китайский язык - ТРЕБУЕТ РАЗРАБОТКИ ⭐⭐⭐

### HanLP (лучший вариант)

**Сайт:** https://hanlp.hankcs.com
**Разработчик:** Harbin Institute of Technology

**Возможности:**
- ✅ Multi-task NLP (PyTorch/TensorFlow 2.x)
- ✅ Сегментация слов (несколько стандартов)
- ✅ POS tagging (включая measure words)
- ✅ Dependency parsing
- ✅ Named Entity Recognition
- ✅ Semantic Role Labeling
- ✅ ~100 MB

**Установка:**
```bash
pip install hanlp
```

**Пример использования:**
```python
import hanlp

HanLP = hanlp.load(hanlp.pretrained.mtl.CLOSE_TOK_POS_NER_SRL_DEP_SDP_CON_ELECTRA_BASE_ZH)

doc = HanLP("他买了三本书")
# Результат:
# {
#   'tok/fine': ['他', '买', '了', '三', '本', '书'],
#   'pos/ctb': ['PN', 'VV', 'AS', 'CD', 'M', 'NN'],
#   ...
# }
```

### Что нужно разработать:

#### 1. Проверка measure words (量词)

**Проблема:** В китайском обязательна конструкция: ЧИСЛО + СЧЁТНОЕ_СЛОВО + СУЩЕСТВИТЕЛЬНОЕ

**Примеры:**
- ✅ Правильно: "三**本**书" (три книги)
- ❌ Ошибка: "三书" (пропущено счётное слово "本")
- ❌ Ошибка: "三**只**书" (неправильное счётное слово, должно быть "本")

**Реализация:**
```python
def _check_measure_words(self, text: str) -> list[ErrorAnnotation]:
    """Check measure word usage using HanLP POS tagging."""
    doc = self._hanlp(text)
    tokens = doc['tok/fine']
    pos_tags = doc['pos/ctb']

    errors = []

    for i in range(len(tokens) - 1):
        # Паттерн: CD (число) + NN (существительное) без MW
        if pos_tags[i] == 'CD':  # Число
            if pos_tags[i + 1].startswith('NN'):  # Существительное
                errors.append(ErrorAnnotation(
                    category="fluency",
                    subcategory="chinese_missing_measure_word",
                    severity=ErrorSeverity.MAJOR,
                    location=(char_pos, char_pos + len(tokens[i]) + len(tokens[i+1])),
                    description=f"Пропущено счётное слово между '{tokens[i]}' и '{tokens[i+1]}'",
                    suggestion="Добавьте счётное слово (个/本/只/条/и т.д.)"
                ))

    return errors
```

**Словарь счётных слов:**
- 个 (gè) - общее (люди, предметы)
- 本 (běn) - книги
- 只 (zhī) - животные
- 条 (tiáo) - длинные предметы (реки, рыбы)
- 张 (zhāng) - плоские предметы (бумага, столы)
- 辆 (liàng) - транспорт
- И т.д. (~100+ счётных слов)

#### 2. Проверка частиц (了的吗呢)

**Типы частиц:**
- **Аспектные:** 了 (le), 过 (guo) - завершённое/пережитое действие
- **Структурные:** 的 (de), 地 (de), 得 (de) - модифицируют существительные/глаголы
- **Модальные:** 吗 (ma), 呢 (ne), 吧 (ba) - вопросы/эмоции

**Проблема:** Специализированной библиотеки НЕ СУЩЕСТВУЕТ

**Решение:** Базовые правила через контекст

```python
def _check_aspect_particles(self, text: str) -> list[ErrorAnnotation]:
    """Check aspect particles 了/过."""
    doc = self._hanlp(text)
    tokens = doc['tok/fine']

    # Индикаторы прошедшего времени
    past_indicators = ["昨天", "以前", "已经", "刚才"]

    has_past = any(ind in tokens for ind in past_indicators)
    has_aspect = any(token in ['了', '过'] for token in tokens)

    if has_past and not has_aspect:
        errors.append(ErrorAnnotation(
            category="fluency",
            subcategory="chinese_missing_aspect_particle",
            severity=ErrorSeverity.MINOR,
            description="Указание на прошедшее время без маркера аспекта (了/过)",
            suggestion="Добавьте 了 или 过 после глагола"
        ))

    return errors
```

**Ограничение:** Использование аспектов/частиц часто стилистическое, не строго обязательное

**Оценка:** ⭐⭐⭐ Feasible, но требует лингвистической экспертизы

---

## Исследования arXiv (2024-2025)

### Ключевые тренды

**1. Adequacy vs Fluency Tradeoff (arXiv:2509.20287, 2025)**
- Большинство метрик (COMET, BLEURT) оптимизированы на adequacy, НЕ fluency
- Рекомендация: использовать отдельные метрики для fluency (FluencyX)
- **Вывод:** Ваш подход (отдельный FluencyAgent) правильный! ✅

**2. Machine Translation Quality Estimation (arXiv:2403.14118, 2024)**
- Эволюция: handcrafted features → deep learning → LLMs
- QE без референсных переводов (оценка в реальном времени)
- **Вывод:** Ваш гибридный NLP+LLM подход - это state-of-the-art 2025! ✅

**3. Multi-Agent MT Systems (2025)**
- Декомпозиция: translation → adequacy → fluency
- Temperature для fluency agents: 0.5 (баланс между deterministic и flexibility)
- **Вывод:** Ваша архитектура RussianFluencyAgent уже следует best practices! ✅

**4. GECToR-2024 (Grammatical Error Correction)**
- State-of-the-art: F0.5 = 66.5
- 10x быстрее seq2seq моделей
- **Вывод:** Не нужен (перекрывается с LLM capabilities)

---

## Сравнительная таблица возможностей

| Функция | Русский (текущий) | Английский (предлагаемый) | Китайский (предлагаемый) |
|---------|-------------------|--------------------------|-------------------------|
| **Морфология** | mawo-pymorphy3 ✅ | spaCy ✅ | HanLP ✅ |
| **Грамматика** | Падежи, вид, согласование ✅ | LanguageTool 5,000+ правил ✅ | Measure words, частицы ⚠️ |
| **NER** | Да ✅ | Да ✅ | Да ✅ |
| **Entity preservation** | Да ✅ | Да ✅ | Да ✅ |
| **Anti-hallucination** | Да ✅ | Да ✅ | Да ✅ |
| **Enrichment для LLM** | Аспекты глаголов, прилаг-сущ пары ✅ | Времена, артикли ✅ | Measure words, частицы ✅ |
| **Размер библиотеки** | ~15 MB ✅ | ~200 MB ⚠️ | ~100 MB ⚠️ |
| **Offline режим** | Да ✅ | Да ✅ | Да ✅ |
| **Зрелость** | Production ✅ | Production ✅ | Production ✅ |

**Легенда:**
- ✅ = Полностью поддерживается / Отлично
- ⚠️ = Частично поддерживается / Требует доработки
- ❌ = Не доступно / Плохо

---

## План работ (поэтапный)

### ✅ Этап 1: Улучшение EnglishLanguageHelper (ПРИОРИТЕТ: ВЫСОКИЙ)

**Оценка времени:** 2-3 часа

**Задачи:**
1. Установить LanguageTool
   ```bash
   pip install language-tool-python
   ```

2. Интегрировать в EnglishLanguageHelper:
   - Инициализация в `__init__()`
   - Реализация `check_grammar()` метода
   - Маппинг ошибок на ErrorAnnotation
   - Фильтрация нерелевантных стилистических замечаний

3. Добавить enrichment данные:
   - Анализ времён глаголов
   - Паттерны артикль-существительное
   - POS распределение

4. Протестировать на 10-20 английских переводах

**Ожидаемый результат:**
- ✅ 5,000+ грамматических правил сразу
- ✅ Детерминированные проверки
- ✅ EnglishLanguageHelper становится мощным

---

### ✅ Этап 2: Валидация EnglishLanguageHelper (ПРИОРИТЕТ: ВЫСОКИЙ)

**Оценка времени:** 1-2 дня

**Задачи:**
1. Запустить бенчмарк-тесты на реальных переводах
2. Измерить precision/recall грамматических проверок
3. Сравнить с базовым FluencyAgent (только LLM)
4. Собрать метрики:
   - Количество найденных ошибок
   - False positive rate
   - Улучшение MQM score

**Критерии успеха:**
- Precision > 80%
- Находит ошибки, которые LLM пропускает
- False positive < 20%

**Решение:**
- ✅ **Если успешно:** Рассмотреть создание EnglishFluencyAgent
- ❌ **Если неудачно:** Остаться на helper-only подходе

---

### ✅ Этап 3: Улучшение ChineseLanguageHelper (ПРИОРИТЕТ: СРЕДНИЙ)

**Оценка времени:** 4-5 часов

**Задачи:**
1. Установить HanLP
   ```bash
   pip install hanlp
   ```

2. Интегрировать в ChineseLanguageHelper:
   - Загрузка multi-task модели
   - Реализация `check_grammar()` метода
   - Проверка measure words
   - Базовая проверка частиц

3. Создать словарь measure words:
   - Основные ~20-30 счётных слов
   - Категории существительных
   - Правила сочетаемости

4. Добавить enrichment данные:
   - Извлечение measure words
   - Извлечение частиц
   - Dependency parsing info

5. Протестировать на китайских переводах

**Ожидаемый результат:**
- ✅ Валидация measure words
- ⚠️ Базовые проверки частиц (warnings, не errors)
- ✅ ChineseLanguageHelper становится сильнее

---

### ✅ Этап 4: Валидация ChineseLanguageHelper (ПРИОРИТЕТ: СРЕДНИЙ)

**Оценка времени:** 1-2 дня

**Задачи:**
1. Запустить бенчмарк-тесты на китайских переводах
2. Измерить эффективность проверки measure words
3. Оценить проверку частиц (может быть много false positives)
4. Собрать feedback от носителей языка (если возможно)

**Критерии успеха:**
- Measure words: precision > 70%
- Частицы: precision > 60% (ниже порог, т.к. сложнее)
- Находит реальные ошибки

**Решение:**
- ✅ **Если успешно:** Рассмотреть создание ChineseFluencyAgent
- ⚠️ **Если частично:** Использовать только measure words, отключить частицы
- ❌ **Если неудачно:** Остаться на helper-only с базовыми проверками

---

### ⏸️ Этап 5: Создание специализированных агентов (НИЗКИЙ ПРИОРИТЕТ)

**ТОЛЬКО ПОСЛЕ успешного завершения Этапов 1-4!**

#### EnglishFluencyAgent

**Создавать ТОЛЬКО ЕСЛИ:**
- ✅ LanguageTool показал реальную пользу (Этап 2)
- ✅ Enrichment данные улучшают LLM промпты
- ✅ Гибридный подход (NLP+LLM) даёт лучшие результаты, чем helper+базовый FluencyAgent

**Архитектура:**
```python
class EnglishFluencyAgent(FluencyAgent):
    """Specialized fluency agent for English language."""

    def __init__(self, llm_provider, helper=None):
        super().__init__(llm_provider)
        self.helper = helper or EnglishLanguageHelper()

    async def evaluate(self, task: TranslationTask):
        if task.target_lang != "en":
            return await super().evaluate(task)

        # Parallel execution
        results = await asyncio.gather(
            self._nlp_check(task),      # LanguageTool
            self._llm_check(task),       # LLM with enrichment
            self._entity_check(task),    # spaCy NER
        )

        # Anti-hallucination verification
        verified_llm = self._verify_llm_errors(llm_errors, text)

        # Merge unique errors
        return base_errors + unique_nlp + verified_llm + entity_errors
```

#### ChineseFluencyAgent

**Создавать ТОЛЬКО ЕСЛИ:**
- ✅ HanLP measure word validation эффективна (Этап 4)
- ✅ Enrichment данные помогают LLM
- ✅ Есть лингвистическая экспертиза для разработки правил

**Архитектура:** Аналогична EnglishFluencyAgent, но с:
- Проверкой measure words
- Базовой проверкой частиц
- Enrichment данными для китайского

---

## Оценка рисков

### Низкий риск ✅ (рекомендуется к немедленной реализации)

**LanguageTool для английского:**
- ✅ Зрелая, стабильная библиотека
- ✅ Open-source с активной поддержкой
- ✅ Хорошо документирована
- ✅ Работает offline
- ⚠️ Большой размер (~200 MB)

**Меры снижения риска:**
- Сделать опциональной зависимостью
- Graceful fallback на spaCy-only режим

---

### Средний риск ⚠️ (требует аккуратной реализации)

**HanLP для китайского:**
- ⚠️ Размер модели (~100 MB)
- ⚠️ Требует PyTorch/TensorFlow
- ⚠️ Правила для measure words требуют лингвистической экспертизы
- ⚠️ Проверка частиц может давать много false positives

**Меры снижения риска:**
- Оставить jieba как лёгкую альтернативу
- Начать с консервативных правил
- Помечать проверки частиц как "suggestions", не "errors"
- Использовать LLM для финальной верификации (гибридный подход)

---

### Высокий риск ❌ (отложить на более поздний этап)

**Преждевременное создание специализированных агентов:**
- ❌ Потраченные усилия, если helpers не дают ценности
- ❌ Нагрузка на поддержку (3 специализированных агента вместо 1 базового)
- ❌ Дублирование кода

**Меры снижения риска:**
- Дождаться результатов валидации Этапов 2 и 4
- Принять решение на основе данных
- Рассмотреть подход helper-only, если достаточно

---

## Итоговые рекомендации

### 🎯 Приоритет 1: Английский (НА ЭТОЙ НЕДЕЛЕ)

**Действия:**
1. Интегрировать LanguageTool в EnglishLanguageHelper
2. Получить 5,000+ грамматических правил немедленно
3. Протестировать на реальных переводах
4. **Высокая отдача при низких затратах**

**Оценка:** ⭐⭐⭐⭐⭐ Максимальный приоритет

---

### 🎯 Приоритет 2: Китайский (СЛЕДУЮЩИЕ 2 НЕДЕЛИ)

**Действия:**
1. Интегрировать HanLP
2. Разработать правила для measure words
3. Добавить базовые проверки частиц
4. **Средняя отдача, требует лингвистической экспертизы**

**Оценка:** ⭐⭐⭐ Важно, но сложнее

---

### 🎯 Приоритет 3: Специализированные агенты (ПОСЛЕ 1-2)

**Действия:**
1. Создавать ТОЛЬКО если helpers стали мощными
2. Валидировать на реальных данных
3. Принять решение на основе метрик

**Оценка:** ⚠️ Зависит от успеха Этапов 1-4

---

## Выводы

### Главные тезисы

1. **RussianFluencyAgent уже оптимален:**
   - Активируется только для русского языка ✅
   - Гибридный NLP+LLM подход - это best practice 2025 ✅
   - Anti-hallucination верификация критически важна ✅

2. **Английский язык имеет готовые решения:**
   - LanguageTool даёт 5,000+ правил немедленно ✅
   - spaCy позволяет кастомные продвинутые правила ✅
   - Высокая уверенность в реализации ✅

3. **Китайский требует больше работы:**
   - HanLP даёт фундамент (POS, parsing) ✅
   - Валидация measure words реализуема ⚠️
   - Проверка частиц требует лингвистической экспертизы ⚠️

4. **Не создавайте агенты пока не усилите helpers:**
   - Сначала улучшите helpers ✅
   - Валидируйте на реальных данных ✅
   - Принимайте решение на основе evidence ✅

---

**Отчёт подготовлен:** 13 ноября 2025
**Источники:** 40+ веб-страниц, 5+ arXiv статей, PyPI, GitHub, научные публикации
**Версия:** 1.0
