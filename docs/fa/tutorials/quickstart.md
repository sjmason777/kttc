# راهنمای شروع سریع

به KTTC خوش آمدید! این آموزش شما را در کمتر از 5 دقیقه از طریق اولین بررسی کیفیت ترجمه خود راهنمایی می‌کند.

## چه چیزی یاد خواهید گرفت

- چگونه KTTC را نصب کنیم
- چگونه کلید API خود را تنظیم کنیم
- چگونه کیفیت ترجمه را بررسی کنیم
- چگونه نتایج را تفسیر کنیم

## پیش‌نیازها

- Python 3.11 یا بالاتر
- یک کلید API از OpenAI یا Anthropic

## مرحله 1: نصب

KTTC را با استفاده از pip نصب کنید:

```bash
pip install kttc
```

این بسته اصلی (~50MB) را نصب می‌کند. برای پیشرفت‌های خاص زبان:

```bash
# بررسی دستور زبان انگلیسی (نیاز به Java 17+ دارد)
pip install kttc[english]

# ویژگی‌های NLP چینی
pip install kttc[chinese]

# ویژگی‌های NLP هندی
pip install kttc[hindi]

# ویژگی‌های NLP فارسی
pip install kttc[persian]

# تمام کمک‌کننده‌های زبان
pip install kttc[all-languages]
```

## مرحله 2: کلید API خود را تنظیم کنید

کلید API ارائه‌دهنده LLM خود را تنظیم کنید:

```bash
# OpenAI (برای مبتدیان توصیه می‌شود)
export KTTC_OPENAI_API_KEY="sk-..."

# یا Anthropic
export KTTC_ANTHROPIC_API_KEY="sk-ant-..."
```

**نکته:** این را به `~/.bashrc` یا `~/.zshrc` خود اضافه کنید تا دائمی شود.

## مرحله 3: فایل‌های تست ایجاد کنید

یک فایل متن منبع ایجاد کنید:

```bash
echo "Hello, world! This is a test." > source.txt
```

یک فایل ترجمه ایجاد کنید:

```bash
echo "سلام دنیا! این یک آزمایش است." > translation.txt
```

## مرحله 4: اولین بررسی کیفیت خود را اجرا کنید

دستور بررسی هوشمند KTTC را اجرا کنید:

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang fa
```

**توجه:** `kttc check` به طور خودکار حالت عملیات را تشخیص می‌دهد:
- فایل واحد → بررسی کیفیت
- چندین ترجمه → مقایسه خودکار
- CSV/JSON → پردازش دسته‌ای

## مرحله 5: نتایج را درک کنید

KTTC خروجی خواهد داد:

```
✓ بررسی کیفیت ترجمه
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ مرحله 1/3: تحلیل زبانی کامل شد
✓ مرحله 2/3: ارزیابی کیفیت کامل شد
✓ مرحله 3/3: گزارش آماده است

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 نتایج ارزیابی کیفیت
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ امتیاز MQM: 96.5 (قبول - کیفیت عالی)

📊 5 عامل ترجمه را تحلیل کردند
⚠️  2 مشکل جزئی، 0 اصلی، 0 حیاتی یافت شد
✓ آستانه کیفیت برآورده شد (≥95.0)
```

### درک امتیاز MQM

- **95-100:** عالی (آماده تولید)
- **90-94:** خوب (نیاز به تعمیرات جزئی)
- **80-89:** قابل قبول (نیاز به بازبینی)
- **<80:** ضعیف (نیاز به بازکاری قابل توجه)

## بعد چه؟

اکنون که اولین بررسی کیفیت خود را اجرا کرده‌اید، کاوش کنید:

- [پردازش دسته‌ای](../guides/batch-processing.md) - پردازش چندین ترجمه
- [اصلاح خودکار](../guides/auto-correction.md) - رفع خودکار خطاهای شناسایی شده
- [واژه‌نامه‌ها](../guides/glossary-management.md) - استفاده از اصطلاحات سفارشی
- [مسیریابی هوشمند](../guides/smart-routing.md) - بهینه‌سازی هزینه‌ها با انتخاب هوشمند مدل

## عیب‌یابی

### خطای "API key not found"

اطمینان حاصل کنید که متغیر محیطی را تنظیم کرده‌اید:

```bash
echo $KTTC_OPENAI_API_KEY
```

اگر خالی است، دوباره آن را تنظیم کنید و در همان جلسه ترمینال امتحان کنید.

### خطای "Module not found"

اطمینان حاصل کنید که KTTC را نصب کرده‌اید:

```bash
pip install kttc
```

برای ویژگی‌های خاص زبان، موارد اضافی را نصب کنید:

```bash
pip install kttc[persian]  # برای ویژگی‌های فارسی
```

### خطای نسخه Python

KTTC به Python 3.11+ نیاز دارد. نسخه خود را بررسی کنید:

```bash
python3 --version
```

اگر 3.11 نصب دارید، به صراحت از آن استفاده کنید:

```bash
python3.11 -m pip install kttc
```

## حالت نمایشی

می‌خواهید KTTC را بدون فراخوانی API امتحان کنید؟

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang fa \
  --demo
```

این از پاسخ‌های شبیه‌سازی شده استفاده می‌کند تا بتوانید بدون هزینه CLI را کاوش کنید.
