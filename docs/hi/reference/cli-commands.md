# CLI कमांड्स संदर्भ

सभी KTTC कमांड-लाइन कमांड्स के लिए पूर्ण संदर्भ।

## kttc check

ऑटो-डिटेक्शन के साथ स्मार्ट अनुवाद गुणवत्ता जाँचकर्ता।

### सिंटैक्स

```bash
kttc check SOURCE [TRANSLATIONS...] [OPTIONS]
```

### ऑटो-डिटेक्शन मोड्स

`kttc check` स्वचालित रूप से पहचानता है कि आप क्या करना चाहते हैं:

| इनपुट | पहचाना गया मोड | व्यवहार |
|-------|--------------|----------|
| `source.txt translation.txt` | सिंगल चेक | गुणवत्ता मूल्यांकन |
| `source.txt trans1.txt trans2.txt` | तुलना | स्वचालित तुलना |
| `translations.csv` | बैच (फ़ाइल) | CSV/JSON प्रोसेस करें |
| `source_dir/ trans_dir/` | बैच (डायरेक्टरी) | डायरेक्टरी प्रोसेस करें |

### विकल्प

#### आवश्यक (सिंगल/तुलना मोड्स के लिए)

- `--source-lang CODE` - स्रोत भाषा कोड (जैसे, `en`)
- `--target-lang CODE` - लक्ष्य भाषा कोड (जैसे, `ru`)

#### स्मार्ट फीचर्स (डिफ़ॉल्ट रूप से सक्षम)

- `--smart-routing` / `--no-smart-routing` - जटिलता-आधारित मॉडल चयन (डिफ़ॉल्ट: सक्षम)
- `--glossary TEXT` - उपयोग करने के लिए शब्दकोश: `auto` (डिफ़ॉल्ट), `none`, या अल्पविराम-पृथक नाम
- `--output PATH` - एक्सटेंशन से स्वतः फ़ॉर्मेट पहचानता है (`.json`, `.md`, `.html`)

#### गुणवत्ता नियंत्रण

- `--threshold FLOAT` - न्यूनतम MQM स्कोर (डिफ़ॉल्ट: 95.0)
- `--auto-correct` - पहचानी गई त्रुटियों को स्वचालित रूप से ठीक करें
- `--correction-level light|full` - सुधार स्तर (डिफ़ॉल्ट: `light`)

#### मॉडल चयन

- `--provider openai|anthropic|gigachat|yandex` - LLM प्रदाता
- `--auto-select-model` - भाषा युग्म के लिए इष्टतम मॉडल उपयोग करें
- `--show-routing-info` - जटिलता विश्लेषण प्रदर्शित करें

#### आउटपुट और वर्बोसिटी

- `--format text|json|markdown|html` - आउटपुट फ़ॉर्मेट (ऑटो-डिटेक्शन को ओवरराइड करता है)
- `--verbose` - विस्तृत आउटपुट दिखाएं
- `--demo` - डेमो मोड (कोई API कॉल नहीं, सिम्युलेटेड प्रतिक्रियाएं)

#### प्रदर्शन और लागत (v1.1 में नया)

- `--quick`, `-q` - त्वरित मोड: 3 कोर एजेंट्स (accuracy, fluency, terminology) के साथ सिंगल पास। सरल टेक्स्ट के लिए तेज़ और सस्ता।
- `--show-cost` - जाँच के बाद टोकन उपयोग और अनुमानित API लागत दिखाएं।

#### तकनीकी दस्तावेज़ीकरण मोड (v1.2 में नया)

KTTC स्वचालित रूप से तकनीकी दस्तावेज़ीकरण (CLI डॉक्स, API संदर्भ, README फ़ाइलें) का पता लगाता है और साहित्यिक शैली विश्लेषण को छोड़ देता है। यह कोड-भारी सामग्री के लिए "चेतना की धारा" या "शब्दाडंबरपूर्ण पैटर्न" जैसी झूठी सकारात्मकता को रोकता है।

**पहचान मार्कर:**
- कोड ब्लॉक्स (` ```bash `, ` ```python `)
- CLI विकल्प (`--option`, `-flag`)
- Markdown शीर्षक और तालिकाएं
- तकनीकी संक्षिप्ताक्षर (API, CLI, SDK, HTTP)
- पर्यावरण चर (KTTC_*, API_KEY)

### MQM गंभीरता स्कोरिंग (v1.1 में अपडेट)

KTTC MQM स्कोरिंग के लिए उद्योग-मानक गंभीरता गुणक का उपयोग करता है:

| गंभीरता | गुणक | प्रभाव |
|---------|------|--------|
| Neutral | 0x | कोई दंड नहीं (सूचनात्मक) |
| Minor | 1x | ध्यान देने योग्य लेकिन समझ को प्रभावित नहीं करता |
| Major | 5x | समझ या गुणवत्ता को प्रभावित करता है |
| Critical | 25x | गंभीर अर्थ परिवर्तन या अनुपयोगी |

**सूत्र:** `गुणवत्ता स्कोर = 100 - (ETPT / शब्द_संख्या × सामान्यीकरण_गुणक)`

जहाँ `ETPT = Σ(त्रुटि_संख्या × गंभीरता_गुणक)`

### उदाहरण

**सिंगल फ़ाइल चेक:**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang es
```

**कई अनुवादों की तुलना करें (ऑटो-डिटेक्टेड):**

```bash
kttc check source.txt trans1.txt trans2.txt trans3.txt \
  --source-lang en \
  --target-lang ru
```

**बैच प्रोसेस CSV (ऑटो-डिटेक्टेड, फ़ाइल से भाषाएं):**

```bash
kttc check translations.csv
```

**बैच प्रोसेस डायरेक्टरीज़:**

```bash
kttc check source_dir/ translation_dir/ \
  --source-lang en \
  --target-lang ru
```

**ऑटो-करेक्शन:**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang ru \
  --auto-correct \
  --correction-level full
```

**HTML रिपोर्ट (एक्सटेंशन से ऑटो-डिटेक्टेड):**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang ru \
  --output report.html
```

**स्मार्ट फीचर्स को अक्षम करें:**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang ru \
  --no-smart-routing \
  --glossary none
```

**डेमो मोड (कोई API कॉल नहीं):**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang es \
  --demo
```

**त्वरित मोड (तेज़, सस्ता):**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang ru \
  --quick
```

**टोकन उपयोग और लागत दिखाएं:**

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang ru \
  --show-cost
```

आउटपुट:
```
✓ अनुवाद गुणवत्ता जांच पास (MQM स्कोर: 96.5)
💰 टोकन: 1,245 (इनपुट: 890, आउटपुट: 355) | कॉल: 5 | लागत: $0.0234
```

---

## kttc batch

कई अनुवादों को बैच प्रोसेस करें।

### सिंटैक्स

**फ़ाइल मोड:**

```bash
kttc batch --file FILE [OPTIONS]
```

**डायरेक्टरी मोड:**

```bash
kttc batch --source-dir DIR --translation-dir DIR \
  --source-lang CODE --target-lang CODE [OPTIONS]
```

### विकल्प

#### मोड चयन (परस्पर अनन्य)

- `--file PATH` - बैच फ़ाइल (CSV, JSON, या JSONL)
- `--source-dir PATH` + `--translation-dir PATH` - डायरेक्टरी मोड

#### आवश्यक (केवल डायरेक्टरी मोड)

- `--source-lang CODE` - स्रोत भाषा कोड
- `--target-lang CODE` - लक्ष्य भाषा कोड

#### सामान्य विकल्प

- `--threshold FLOAT` - न्यूनतम MQM स्कोर (डिफ़ॉल्ट: 95.0)
- `--output PATH` - आउटपुट रिपोर्ट पथ (डिफ़ॉल्ट: `report.json`)
- `--parallel INT` - समानांतर कार्यकर्ताओं की संख्या (डिफ़ॉल्ट: 4)
- `--glossary TEXT` - उपयोग करने के लिए शब्दकोश
- `--smart-routing` - जटिलता-आधारित रूटिंग सक्षम करें
- `--show-progress` / `--no-progress` - प्रगति बार दिखाएं (डिफ़ॉल्ट: दिखाएं)
- `--verbose` - वर्बोस आउटपुट
- `--demo` - डेमो मोड

#### केवल फ़ाइल मोड

- `--batch-size INT` - समूहन के लिए बैच आकार

### समर्थित फ़ाइल स्वरूप

**CSV:**

```csv
source,translation,source_lang,target_lang,domain
"Hello world","Hola mundo","en","es","general"
```

**JSON:**

```json
[
  {
    "source": "Hello world",
    "translation": "Hola mundo",
    "source_lang": "en",
    "target_lang": "es",
    "domain": "general"
  }
]
```

**JSONL:**

```jsonl
{"source": "Hello world", "translation": "Hola mundo", "source_lang": "en", "target_lang": "es"}
{"source": "Good morning", "translation": "Buenos días", "source_lang": "en", "target_lang": "es"}
```

### उदाहरण

**CSV फ़ाइल प्रोसेस करें:**

```bash
kttc batch --file translations.csv
```

**प्रगति के साथ JSON प्रोसेस करें:**

```bash
kttc batch --file translations.json \
  --show-progress \
  --output results.json
```

**डायरेक्टरी मोड:**

```bash
kttc batch \
  --source-dir ./source \
  --translation-dir ./translations \
  --source-lang en \
  --target-lang es \
  --parallel 8
```

---

## kttc compare

कई अनुवादों की साथ-साथ तुलना करें।

### सिंटैक्स

```bash
kttc compare --source FILE \
  --translation FILE --translation FILE [...] \
  --source-lang CODE --target-lang CODE [OPTIONS]
```

### विकल्प

- `--source PATH` - स्रोत पाठ फ़ाइल (आवश्यक)
- `--translation PATH` - अनुवाद फ़ाइल (कई बार निर्दिष्ट की जा सकती है, आवश्यक)
- `--source-lang CODE` - स्रोत भाषा कोड (आवश्यक)
- `--target-lang CODE` - लक्ष्य भाषा कोड (आवश्यक)
- `--threshold FLOAT` - गुणवत्ता सीमा (डिफ़ॉल्ट: 95.0)
- `--provider TEXT` - LLM प्रदाता
- `--verbose` - विस्तृत तुलना दिखाएं

### उदाहरण

**3 अनुवादों की तुलना करें:**

```bash
kttc compare \
  --source text.txt \
  --translation trans1.txt \
  --translation trans2.txt \
  --translation trans3.txt \
  --source-lang en \
  --target-lang ru \
  --verbose
```

---

## kttc translate

स्वचालित गुणवत्ता जाँच और परिष्करण के साथ पाठ का अनुवाद करें।

### सिंटैक्स

```bash
kttc translate --text TEXT \
  --source-lang CODE --target-lang CODE [OPTIONS]
```

### विकल्प

- `--text TEXT` - अनुवाद करने के लिए पाठ (या फ़ाइल इनपुट के लिए `@file.txt`, आवश्यक)
- `--source-lang CODE` - स्रोत भाषा कोड (आवश्यक)
- `--target-lang CODE` - लक्ष्य भाषा कोड (आवश्यक)
- `--threshold FLOAT` - परिष्करण के लिए गुणवत्ता सीमा (डिफ़ॉल्ट: 95.0)
- `--max-iterations INT` - अधिकतम परिष्करण पुनरावृत्तियाँ (डिफ़ॉल्ट: 3)
- `--output PATH` - आउटपुट फ़ाइल पथ
- `--provider TEXT` - LLM प्रदाता
- `--verbose` - वर्बोस आउटपुट

### उदाहरण

**इनलाइन पाठ का अनुवाद करें:**

```bash
kttc translate --text "Hello, world!" \
  --source-lang en \
  --target-lang es
```

**फ़ाइल से अनुवाद करें:**

```bash
kttc translate --text @document.txt \
  --source-lang en \
  --target-lang ru \
  --output translated.txt
```

**गुणवत्ता सीमा के साथ:**

```bash
kttc translate --text "Complex technical text" \
  --source-lang en \
  --target-lang zh \
  --threshold 98 \
  --max-iterations 5
```

---

## kttc benchmark

कई LLM प्रदाताओं का बेंचमार्क करें।

### सिंटैक्स

```bash
kttc benchmark --source FILE \
  --source-lang CODE --target-lang CODE \
  --providers LIST [OPTIONS]
```

### विकल्प

- `--source PATH` - स्रोत पाठ फ़ाइल (आवश्यक)
- `--source-lang CODE` - स्रोत भाषा कोड (आवश्यक)
- `--target-lang CODE` - लक्ष्य भाषा कोड (आवश्यक)
- `--providers TEXT` - अल्पविराम-पृथक प्रदाता सूची (डिफ़ॉल्ट: `gigachat,openai,anthropic`)
- `--threshold FLOAT` - गुणवत्ता सीमा (डिफ़ॉल्ट: 95.0)
- `--output PATH` - आउटपुट फ़ाइल पथ (JSON)
- `--verbose` - वर्बोस आउटपुट

### उदाहरण

**सभी प्रदाताओं का बेंचमार्क करें:**

```bash
kttc benchmark \
  --source text.txt \
  --source-lang en \
  --target-lang ru \
  --providers gigachat,openai,anthropic
```

---

## kttc report

QA परिणामों से स्वरूपित रिपोर्ट जनरेट करें।

### सिंटैक्स

```bash
kttc report INPUT_FILE [OPTIONS]
```

### विकल्प

- `--format markdown|html` - आउटपुट फ़ॉर्मेट (डिफ़ॉल्ट: markdown)
- `--output PATH` - आउटपुट फ़ाइल पथ (निर्दिष्ट न होने पर ऑटो-जनरेटेड)

### उदाहरण

**Markdown रिपोर्ट जनरेट करें:**

```bash
kttc report results.json --format markdown -o report.md
```

**HTML रिपोर्ट जनरेट करें:**

```bash
kttc report results.json --format html -o report.html
```

---

## kttc glossary

प्रोजेक्ट-लोकल और यूज़र-ग्लोबल स्टोरेज दोनों के लिए समर्थन के साथ शब्दावली शब्दकोशों का प्रबंधन करें।

### स्टोरेज स्थान

KTTC दो-स्तरीय शब्दकोश स्टोरेज का समर्थन करता है:

- **प्रोजेक्ट शब्दकोश** (डिफ़ॉल्ट): `./glossaries/` - वर्तमान प्रोजेक्ट में संग्रहीत, संस्करण-नियंत्रित किया जा सकता है
- **यूज़र शब्दकोश** (`--user` फ्लैग के साथ): `~/.kttc/glossaries/` - सभी प्रोजेक्ट्स में उपलब्ध ग्लोबल शब्दकोश

**खोज प्राथमिकता**: पहले प्रोजेक्ट शब्दकोश जाँचे जाते हैं, फिर यूज़र शब्दकोश।

### सबकमांड्स

#### list

दोनों स्थानों से सभी उपलब्ध शब्दकोश सूचीबद्ध करें:

```bash
kttc glossary list
```

दिखाता है: नाम, स्थान (प्रोजेक्ट/यूज़र), शब्द गणना, और फ़ाइल पथ।

#### show

शब्दकोश सामग्री दिखाएं:

```bash
kttc glossary show NAME [OPTIONS]
```

**विकल्प:**
- `--lang-pair SRC-TGT` - भाषा युग्म द्वारा फ़िल्टर करें (जैसे, `en-ru`)
- `--limit N` - दिखाई गई प्रविष्टियों की संख्या सीमित करें

#### create

CSV या JSON फ़ाइल से एक नया शब्दकोश बनाएं:

```bash
kttc glossary create NAME --from-csv FILE
# या
kttc glossary create NAME --from-json FILE
```

**विकल्प:**
- `--from-csv PATH` - CSV फ़ाइल से बनाएं (`--from-json` उपयोग न करने पर आवश्यक)
- `--from-json PATH` - JSON फ़ाइल से बनाएं (`--from-csv` उपयोग न करने पर आवश्यक)
- `--user` - प्रोजेक्ट डायरेक्टरी के बजाय यूज़र डायरेक्टरी (`~/.kttc/glossaries/`) में सहेजें

**CSV स्वरूप** (आवश्यक कॉलम):

```csv
source,target,source_lang,target_lang,context,notes
API,API,en,es,Keep as-is,Technical term
database,base de datos,en,es,,
```

**JSON स्वरूप:**

```json
{
  "metadata": {
    "name": "technical",
    "description": "Technical terminology",
    "version": "1.0.0"
  },
  "entries": [
    {
      "source": "API",
      "target": "API",
      "source_lang": "en",
      "target_lang": "es",
      "context": "Keep as-is",
      "notes": "Technical term"
    }
  ]
}
```

#### merge

कई शब्दकोशों को एक में मर्ज करें:

```bash
kttc glossary merge GLOSSARY1 GLOSSARY2 [...] --output NAME [OPTIONS]
```

**विकल्प:**
- `--output NAME` - आउटपुट शब्दकोश नाम (आवश्यक)
- `--user` - मर्ज किए गए शब्दकोश को यूज़र डायरेक्टरी में सहेजें

#### export

शब्दकोश को CSV या JSON में एक्सपोर्ट करें:

```bash
kttc glossary export NAME [OPTIONS]
```

**विकल्प:**
- `--format csv|json` - एक्सपोर्ट फ़ॉर्मेट (डिफ़ॉल्ट: csv)
- `--output PATH` - आउटपुट फ़ाइल पथ (डिफ़ॉल्ट: `{name}.{format}`)

#### validate

शब्दकोश फ़ाइल स्वरूप को मान्य करें:

```bash
kttc glossary validate FILE
```

जाँचता है:
- आवश्यक फ़ील्ड (source, target, source_lang, target_lang)
- डुप्लिकेट प्रविष्टियाँ
- खाली मान
- मान्य भाषा कोड

### उदाहरण

**सभी शब्दकोश सूचीबद्ध करें (प्रोजेक्ट + यूज़र):**

```bash
kttc glossary list
```

आउटपुट:
```
📚 Project Glossaries (./glossaries/):
  • base (120 terms) - ./glossaries/base.json
  • technical (45 terms) - ./glossaries/technical.json

📚 User Glossaries (~/.kttc/glossaries/):
  • personal (30 terms) - ~/.kttc/glossaries/personal.json
```

**CSV से प्रोजेक्ट शब्दकोश बनाएं:**

```bash
kttc glossary create medical --from-csv medical-terms.csv
```

`./glossaries/medical.json` में सहेजता है (git में कमिट किया जा सकता है)।

**ग्लोबल यूज़र शब्दकोश बनाएं:**

```bash
kttc glossary create personal --from-csv my-terms.csv --user
```

`~/.kttc/glossaries/personal.json` में सहेजता है (सभी प्रोजेक्ट्स में उपलब्ध)।

**फ़िल्टरिंग के साथ शब्दकोश दिखाएं:**

```bash
kttc glossary show base --lang-pair en-ru --limit 10
```

**कई शब्दकोश मर्ज करें:**

```bash
kttc glossary merge base technical medical --output combined
```

तीन शब्दकोशों से सभी शब्दों के साथ `./glossaries/combined.json` बनाता है।

**यूज़र डायरेक्टरी में मर्ज करें:**

```bash
kttc glossary merge base technical --output my-combined --user
```

`~/.kttc/glossaries/my-combined.json` बनाता है।

**CSV में एक्सपोर्ट करें:**

```bash
kttc glossary export technical --format csv --output technical-export.csv
```

**शब्दकोश फ़ाइल मान्य करें:**

```bash
kttc glossary validate my-glossary.csv
```

आउटपुट:
```
✓ All required columns present
✓ No duplicate entries found
✓ All language codes valid
✓ No empty values
✅ Glossary is valid
```

### अनुवाद जाँच में शब्दकोशों का उपयोग करना

`kttc check` में नाम से शब्दकोशों का संदर्भ दें:

```bash
# 'base' शब्दकोश ऑटो-डिटेक्ट करें (प्रोजेक्ट, फिर यूज़र खोजता है)
kttc check source.txt trans.txt --source-lang en --target-lang ru --glossary auto

# विशिष्ट शब्दकोश उपयोग करें (अल्पविराम-पृथक)
kttc check source.txt trans.txt --source-lang en --target-lang ru --glossary base,technical,medical

# शब्दकोश अक्षम करें
kttc check source.txt trans.txt --source-lang en --target-lang ru --glossary none
```

**खोज क्रम**: KTTC पहले प्रोजेक्ट डायरेक्टरी में शब्दकोश खोजता है, फिर यूज़र डायरेक्टरी में।

---

## वैश्विक विकल्प

सभी कमांड्स के लिए उपलब्ध:

- `--ui-lang CODE`, `-L CODE` - CLI इंटरफ़ेस भाषा (en, ru, zh, hi, fa) या सिस्टम भाषा पहचान के लिए 'auto'
- `--version`, `-v` - संस्करण दिखाएं और बाहर निकलें
- `--help` - सहायता संदेश दिखाएं

---

## एग्ज़िट कोड्स

- `0` - सफलता (सभी अनुवादों ने गुणवत्ता सीमा पार की)
- `1` - विफलता (एक या अधिक अनुवाद गुणवत्ता सीमा में विफल)
- `130` - उपयोगकर्ता द्वारा बाधित (Ctrl+C)

---

## पर्यावरण चर

- `KTTC_OPENAI_API_KEY` - OpenAI API कुंजी
- `KTTC_ANTHROPIC_API_KEY` - Anthropic API कुंजी
- `KTTC_GIGACHAT_CLIENT_ID` - GigaChat क्लाइंट ID
- `KTTC_GIGACHAT_CLIENT_SECRET` - GigaChat क्लाइंट सीक्रेट
- `KTTC_YANDEX_API_KEY` - Yandex GPT API कुंजी
- `KTTC_YANDEX_FOLDER_ID` - Yandex GPT फ़ोल्डर ID

---

## यह भी देखें

- [CLI उपयोग गाइड](../guides/cli-usage.md) - व्यावहारिक उदाहरण
- [कॉन्फ़िगरेशन](../guides/configuration.md) - उन्नत कॉन्फ़िगरेशन
- [API संदर्भ](api-reference.md) - Python API
