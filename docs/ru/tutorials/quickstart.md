# Руководство по быстрому началу

Добро пожаловать в KTTC! Это руководство поможет вам провести первую проверку качества перевода менее чем за 5 минут.

## Что вы изучите

- Как установить KTTC
- Как настроить ваш API-ключ
- Как проверить качество перевода
- Как интерпретировать результаты

## Требования

- Python 3.11 или выше
- API-ключ OpenAI или Anthropic

## Шаг 1: Установка

Установите KTTC с помощью pip:

```bash
pip install kttc
```

Это установит основной пакет (~50 МБ). Для языковых улучшений:

```bash
# Проверка английской грамматики (требует Java 17+)
pip install kttc[english]

# Функции NLP для китайского языка
pip install kttc[chinese]

# Все языковые помощники
pip install kttc[all-languages]
```

## Шаг 2: Установите ваш API-ключ

Установите API-ключ вашего поставщика LLM:

```bash
# OpenAI (рекомендуется для начинающих)
export KTTC_OPENAI_API_KEY="sk-..."

# Или Anthropic
export KTTC_ANTHROPIC_API_KEY="sk-ant-..."
```

**Совет:** Добавьте это в ваш `~/.bashrc` или `~/.zshrc`, чтобы сделать постоянным.

## Шаг 3: Создайте тестовые файлы

Создайте файл с исходным текстом:

```bash
echo "Hello, world! This is a test." > source.txt
```

Создайте файл с переводом:

```bash
echo "¡Hola, mundo! Esto es una prueba." > translation.txt
```

## Шаг 4: Выполните первую проверку качества

Запустите команду умной проверки KTTC:

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang es
```

**Примечание:** `kttc check` автоматически определяет режим работы:
- Один файл → проверка качества
- Несколько переводов → автоматическое сравнение
- CSV/JSON → пакетная обработка

## Шаг 5: Понимание результатов

KTTC выведет:

```
✓ Translation Quality Check
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ Step 1/3: Linguistic analysis complete
✓ Step 2/3: Quality assessment complete
✓ Step 3/3: Report ready

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Quality Assessment Results
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ MQM Score: 96.5 (PASS - Excellent Quality)

📊 5 agents analyzed translation
⚠️  Found 2 minor issues, 0 major, 0 critical
✓ Quality threshold met (≥95.0)
```

### Понимание MQM оценки

- **95-100:** Отличное (готово к производству)
- **90-94:** Хорошее (требуются небольшие исправления)
- **80-89:** Приемлемое (требуется доработка)
- **<80:** Плохое (требуется значительная переработка)

## Что дальше?

Теперь, когда вы выполнили первую проверку качества, изучите:

- [Пакетная обработка](../guides/batch-processing.md) - Обрабатывайте несколько переводов
- [Автокоррекция](../guides/auto-correction.md) - Автоматически исправляйте обнаруженные ошибки
- [Глоссарии](../guides/glossary-management.md) - Используйте пользовательскую терминологию
- [Умная маршрутизация](../guides/smart-routing.md) - Оптимизируйте затраты с интеллектуальным выбором моделей

## Устранение неполадок

### Ошибка "API key not found"

Убедитесь, что вы установили переменную окружения:

```bash
echo $KTTC_OPENAI_API_KEY
```

Если пустая, установите ее снова и попробуйте в той же сессии терминала.

### Ошибка "Module not found"

Убедитесь, что вы установили KTTC:

```bash
pip install kttc
```

Для языковых функций установите дополнения:

```bash
pip install kttc[english]  # Для LanguageTool
```

### Ошибка версии Python

KTTC требует Python 3.11+. Проверьте вашу версию:

```bash
python3 --version
```

Если у вас установлен 3.11, используйте его явно:

```bash
python3.11 -m pip install kttc
```

## Демо режим

Хотите попробовать KTTC без API-вызовов?

```bash
kttc check source.txt translation.txt \
  --source-lang en \
  --target-lang es \
  --demo
```

Это использует симулированные ответы, так что вы можете изучить CLI без затрат.